version: "3.5"
services:
  grafana:
    image: grafana/grafana:latest
    user: "1000"
    ports:
      - 3009:3000
    volumes:
      - ./data:/var/lib/grafana

networks:
  default:
    external:
      name: local-sb-network
